<p id="notice"><%= notice %></p>

<%= @user.phone_codes.each do |phone_code| %>
  
<% end %>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to "confirm", "/users/confirmation/new" %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
